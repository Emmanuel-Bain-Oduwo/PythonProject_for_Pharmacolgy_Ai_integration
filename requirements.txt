#Core web framework and server
fastapi>=0.111.0,<1.0.0
uvicorn[standard]>=0.29.0,<1.0.0

# AI — MUST be 1.x, NOT 2.x (2.x has breaking API changes) 
openai>=1.30.0,<2.0.0

# ── HTTP client — pinned; openai 1.x needs httpx 0.27.x ─────────────────────
httpx>=0.27.0,<0.28.0

# ── Environment ───────────────────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── Data / ML ─────────────────────────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.4.0
joblib>=1.3.0

# ── Visualisation ─────────────────────────────────────────────────────────────
plotly>=5.20.0
matplotlib>=3.8.0
seaborn>=0.13.0

# ── API validation ────────────────────────────────────────────────────────────

